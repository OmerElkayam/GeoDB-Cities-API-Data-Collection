# 🌍 GeoDB Cities Data Collection

This project uses the **GeoDB Cities API** (via RapidAPI) to collect, process, and export global city data.

## 📊 Overview
The notebook demonstrates how to:
- Send API requests to the GeoDB Cities endpoint  
- Parse and store results as JSON  
- Convert API results into a Pandas DataFrame  
- Export the data to CSV for further analysis

## 🧰 Technologies Used
- Python  
- Requests  
- Pandas  
- JSON  
- RapidAPI (GeoDB Cities)

## 🚀 How to Run
1. Install dependencies:
   ```bash
   pip install requests pandas
